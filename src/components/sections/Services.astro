---
// Services Section - Los 4 Pilares
const services = [
  {
    icon: '🌐',
    title: 'Desarrollo Web de Alto Rendimiento',
    description: 'Sitios estáticos y dinámicos ultra-rápidos. Sin plugins costosos.',
    features: ['Landing Pages', 'Sitios Corporativos', 'One-Page Apps'],
    color: 'blue',
    href: '/servicios/desarrollo-web'
  },
  {
    icon: '🤖',
    title: 'Automatización y Eficiencia (RPA)',
    description: '"Recupera 10 horas de tu semana". Bots en Python para tareas repetitivas.',
    features: ['Bots Python', 'Integraciones API', 'Scripts Automatizados'],
    color: 'purple',
    href: '/servicios/automatizacion-rpa'
  },
  {
    icon: '☁️',
    title: 'Soluciones Empresariales y Cloud',
    description: 'Migración a AWS, Correos Corporativos, Gestión de trabajadores.',
    features: ['AWS Migration', 'Email Corporativo', 'Gestión RRHH'],
    color: 'orange',
    href: '/servicios/soluciones-cloud'
  },
  {
    icon: '📱',
    title: 'Desarrollo a Medida',
    description: 'Apps Móviles Nativas y Sistemas SaaS personalizados.',
    features: ['Apps iOS/Android', 'Sistemas SaaS', 'APIs Custom'],
    color: 'green',
    href: '/servicios/desarrollo-medida'
  },
];

const colorClasses = {
  blue: 'bg-blue-100 text-blue-600 group-hover:bg-blue-600',
  purple: 'bg-purple-100 text-purple-600 group-hover:bg-purple-600',
  orange: 'bg-orange-100 text-orange-600 group-hover:bg-orange-600',
  green: 'bg-green-100 text-green-600 group-hover:bg-green-600',
};

const borderClasses = {
  blue: 'group-hover:border-blue-200',
  purple: 'group-hover:border-purple-200',
  orange: 'group-hover:border-orange-200',
  green: 'group-hover:border-green-200',
};
---

<section id="servicios" class="section-padding bg-white">
  <div class="container-custom">
    <!-- Header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <span class="inline-block px-4 py-1.5 bg-blue-100 text-blue-700 rounded-full text-sm font-medium mb-6">
        Nuestros Servicios
      </span>
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-slate-900 mb-6">
        Los 4 Pilares de Nuestra Oferta
      </h2>
      <p class="text-lg text-slate-600">
        Soluciones tecnológicas integrales diseñadas para impulsar el crecimiento de tu empresa.
      </p>
    </div>

    <!-- Services Grid -->
    <div class="grid md:grid-cols-2 gap-6 lg:gap-8">
      {services.map((service) => (
        <div class={`group relative bg-white rounded-2xl p-8 border-2 border-slate-100 ${(borderClasses as any)[service.color]} transition-all hover:shadow-xl hover:-translate-y-1`}>
          <!-- Icon -->
          <div class={`w-16 h-16 ${(colorClasses as any)[service.color]} rounded-2xl flex items-center justify-center text-3xl mb-6 transition-all group-hover:text-white group-hover:scale-110`}>
            {service.icon}
          </div>

          <!-- Content -->
          <h3 class="text-xl lg:text-2xl font-bold text-slate-900 mb-3">
            {service.title}
          </h3>
          <p class="text-slate-600 mb-6">
            {service.description}
          </p>

          <!-- Features -->
          <div class="flex flex-wrap gap-2">
            {service.features.map((feature) => (
              <span class="px-3 py-1 bg-slate-100 text-slate-600 rounded-full text-sm">
                {feature}
              </span>
            ))}
          </div>

          <!-- Arrow -->
          <a 
            href={service.href} 
            class="absolute top-8 right-8 w-10 h-10 bg-slate-100 rounded-full flex items-center justify-center text-slate-400 group-hover:bg-blue-600 group-hover:text-white transition-all"
            aria-label={`Más información sobre ${service.title}`}
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
        </div>
      ))}
    </div>

    <!-- Bottom Banner -->
    <div class="mt-16 bg-gradient-to-r from-slate-900 to-slate-800 rounded-2xl p-8 lg:p-12 text-center">
      <h3 class="text-2xl lg:text-3xl font-bold text-white mb-4">
        ¿Necesitas una solución personalizada?
      </h3>
      <p class="text-slate-300 mb-8 max-w-2xl mx-auto">
        Cada negocio es único. Conversemos sobre cómo podemos adaptar nuestra tecnología a tus necesidades específicas.
      </p>
      <a 
        href="#contacto" 
        class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-white text-slate-900 font-semibold rounded-xl hover:bg-blue-50 transition-all"
      >
        Solicitar Evaluación Gratuita
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>
    </div>
  </div>
</section>
