---
// Pricing Section - Tabbed Component with Market Research Based Pricing

const monthlyPlans = [
  {
    name: 'Plan Despegue',
    price: '$19.990',
    period: '/ mes',
    ideal: 'Emprendedores e ideas nuevas',
    features: [
      'One-Page (Landing)',
      'Dominio .CL incluido',
      'Hosting AWS Serverless',
      'Certificado SSL',
      'Botón WhatsApp',
    ],
    details: [
      'Sin cuota de alta ni contratos largos',
      'Carga en menos de 0.5 segundos',
      'Sin plugins vulnerables',
      'Soporte por email',
      'Cambios menores incluidos',
    ],
    popular: false,
    cta: 'Comenzar Ahora',
  },
  {
    name: 'Plan Crecimiento',
    price: '$34.990',
    period: '/ mes',
    ideal: 'Pymes y Servicios',
    features: [
      'Todo lo de Despegue +',
      'Hasta 5 secciones',
      'Correos Corporativos',
      'SEO Básico',
      'Blog Autoadministrable',
    ],
    details: [
      'Panel de administración intuitivo',
      'Optimización para Google',
      'Hasta 5 correos @tuempresa.cl',
      'Backups automáticos',
      'Soporte prioritario',
    ],
    popular: true,
    cta: 'Elegir Crecimiento',
  },
  {
    name: 'Catálogo Pro',
    price: '$49.990',
    period: '/ mes',
    ideal: 'Ventas y Catálogos',
    features: [
      'Todo lo de Crecimiento +',
      'Catálogo hasta 50 productos',
      'Buscador inteligente',
      'Panel de gestión a medida',
      'Integración WhatsApp Business',
    ],
    details: [
      'Gestión de inventario básica',
      'Filtros de búsqueda avanzados',
      'Galería de imágenes optimizada',
      'Notificaciones de pedidos',
      'Reportes de visitas',
    ],
    popular: false,
    cta: 'Elegir Catálogo',
  },
];

const annualPlans = [
  {
    name: 'Despegue Anual',
    price: '$199.990',
    period: '/ año',
    ideal: 'Emprendedores e ideas nuevas',
    savings: '¡2 meses gratis!',
    features: [
      'One-Page (Landing)',
      'Dominio .CL incluido',
      'Hosting AWS Serverless',
      'Certificado SSL',
      'Botón WhatsApp',
    ],
    details: [
      'Ahorro de $39.880 vs mensual',
      'Carga en menos de 0.5 segundos',
      'Sin plugins vulnerables',
      'Soporte por email',
      'Cambios menores incluidos',
    ],
    popular: false,
    cta: 'Elegir Anual',
  },
  {
    name: 'Crecimiento Anual',
    price: '$349.990',
    period: '/ año',
    ideal: 'Pymes y Servicios',
    savings: 'Mejor opción Pyme',
    features: [
      'Todo lo de Despegue +',
      'Hasta 5 secciones',
      'Correos Corporativos',
      'SEO Básico',
      'Blog Autoadministrable',
    ],
    details: [
      'Ahorro de $69.890 vs mensual',
      'Panel de administración intuitivo',
      'Optimización para Google',
      'Hasta 5 correos @tuempresa.cl',
      'Soporte prioritario 24/7',
    ],
    popular: true,
    cta: 'Mejor Opción',
  },
  {
    name: 'Catálogo Pro Anual',
    price: '$499.990',
    period: '/ año',
    ideal: 'Ventas y Catálogos',
    savings: 'Ahorra $99.890',
    features: [
      'Todo lo de Crecimiento +',
      'Catálogo hasta 50 productos',
      'Buscador inteligente',
      'Panel de gestión a medida',
      'Integración WhatsApp Business',
    ],
    details: [
      'Ahorro de $99.890 vs mensual',
      'Gestión de inventario',
      'Filtros avanzados',
      'Notificaciones de pedidos',
      'Reportes completos',
    ],
    popular: false,
    cta: 'Elegir Pro Anual',
  },
];

const oneTimePlans = [
  {
    name: 'Landing Page Propia',
    price: '$350.000',
    period: '+ IVA (único)',
    ideal: 'Tú eres dueño del código',
    features: [
      'Diseño exclusivo en Rust/HTML',
      'Optimización 100/100 Google',
      'Código fuente entregado',
      'Integración AWS propia',
      'Documentación técnica',
    ],
    details: [
      'Propiedad total del código',
      'Sin dependencias de terceros',
      'Arquitectura escalable',
      'Hosting por tu cuenta o gestionado (1 UF/mes)',
      'Mantenimiento opcional',
    ],
    popular: false,
    cta: 'Solicitar Cotización',
  },
  {
    name: 'Corporativo A Medida',
    price: '$650.000',
    period: '+ IVA (único)',
    ideal: 'Empresas establecidas',
    features: [
      'Desarrollo Full Stack',
      'Base de datos optimizada',
      'Arquitectura escalable',
      'Panel admin personalizado',
      'Integraciones a medida',
    ],
    details: [
      'Desarrollo completo Rust + AWS',
      'Base de datos DynamoDB/PostgreSQL',
      'API REST documentada',
      'Capacitación incluida',
      'Soporte post-lanzamiento 30 días',
    ],
    popular: true,
    cta: 'Agendar Reunión',
  },
];
---

<section id="precios" class="section-padding bg-slate-50">
  <div class="container-custom">
    <!-- Header -->
    <div class="text-center max-w-3xl mx-auto mb-12">
      <span class="inline-block px-4 py-1.5 bg-blue-100 text-blue-700 rounded-full text-sm font-medium mb-6">
        Precios Competitivos
      </span>
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-slate-900 mb-4">
        Planes Flexibles para cada etapa de tu negocio
      </h2>
      <p class="text-lg text-slate-600">
        Elige cómo quieres pagar: Arriendo mensual, Ahorro anual o Compra única.
      </p>
    </div>

    <!-- Tabs Navigation -->
    <div class="flex justify-center mb-10">
      <div class="inline-flex flex-wrap justify-center gap-2 p-1.5 bg-white rounded-xl shadow-sm border border-slate-200">
        <button 
          data-tab="mensual" 
          class="tab-btn active px-4 sm:px-6 py-2.5 rounded-lg font-medium text-sm transition-all"
        >
          Mensual
        </button>
        <button 
          data-tab="anual" 
          class="tab-btn px-4 sm:px-6 py-2.5 rounded-lg font-medium text-sm transition-all"
        >
          <span class="hidden sm:inline">Anual</span>
          <span class="sm:hidden">Anual</span>
          <span class="ml-1 text-xs bg-green-100 text-green-700 px-2 py-0.5 rounded-full">-2 meses</span>
        </button>
        <button 
          data-tab="unico" 
          class="tab-btn px-4 sm:px-6 py-2.5 rounded-lg font-medium text-sm transition-all"
        >
          Pago Único
        </button>
        <button 
          data-tab="personalizado" 
          class="tab-btn px-4 sm:px-6 py-2.5 rounded-lg font-medium text-sm transition-all"
        >
          Personalizado
        </button>
      </div>
    </div>

    <!-- Tab Content: Mensual -->
    <div id="tab-mensual" class="tab-content">
      <div class="grid md:grid-cols-3 gap-6 lg:gap-8 max-w-6xl mx-auto">
        {monthlyPlans.map((plan, index) => (
          <div class={`relative bg-white rounded-2xl p-6 lg:p-8 border-2 ${plan.popular ? 'border-orange-500 shadow-2xl shadow-orange-500/10 scale-105 z-10' : 'border-slate-200'} transition-all hover:shadow-xl`}>
            {plan.popular && (
              <div class="absolute -top-4 left-1/2 -translate-x-1/2">
                <span class="px-4 py-1.5 bg-orange-700 text-white text-sm font-semibold rounded-full">
                  Más Popular
                </span>
              </div>
            )}

            <div class="text-center mb-6">
              <h3 class="text-xl font-bold text-slate-900 mb-1">{plan.name}</h3>
              <p class="text-slate-500 text-sm">{plan.ideal}</p>
            </div>

            <div class="text-center mb-6">
              <span class="text-4xl font-bold text-slate-900">{plan.price}</span>
              <span class="text-slate-500">{plan.period}</span>
              <p class="text-xs text-green-700 mt-1">Sin cuota de alta</p>
            </div>

            <ul class="space-y-3 mb-6">
              {plan.features.map((feature) => (
                <li class="flex items-center gap-3 text-slate-600">
                  <svg class="w-5 h-5 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                  </svg>
                  <span class="text-sm">{feature}</span>
                </li>
              ))}
            </ul>

            <a 
              href="#contacto" 
              class={`block text-center py-3 rounded-xl font-semibold transition-all ${
                plan.popular 
                  ? 'bg-gradient-to-r from-orange-500 to-orange-600 text-white hover:from-orange-600 hover:to-orange-700 shadow-lg shadow-orange-500/25' 
                  : 'bg-slate-100 text-slate-700 hover:bg-slate-200'
              }`}
            >
              {plan.cta}
            </a>

            <button 
              data-modal={`modal-mensual-${index}`}
              class="w-full mt-3 text-center text-sm text-blue-600 hover:text-blue-700 font-medium"
            >
              Ver detalles completos ->
            </button>
          </div>
        ))}
      </div>
    </div>

    <!-- Tab Content: Anual -->
    <div id="tab-anual" class="tab-content hidden">
      <div class="grid md:grid-cols-3 gap-6 lg:gap-8 max-w-6xl mx-auto">
        {annualPlans.map((plan, index) => (
          <div class={`relative bg-white rounded-2xl p-6 lg:p-8 border-2 ${plan.popular ? 'border-blue-500 shadow-2xl shadow-blue-500/10 scale-105 z-10' : 'border-slate-200'} transition-all hover:shadow-xl`}>
            {plan.popular && (
              <div class="absolute -top-4 left-1/2 -translate-x-1/2">
                <span class="px-4 py-1.5 bg-blue-600 text-white text-sm font-semibold rounded-full">
                  Mejor Opción
                </span>
              </div>
            )}

            <div class="text-center mb-6">
              <h3 class="text-xl font-bold text-slate-900 mb-1">{plan.name}</h3>
              <p class="text-slate-500 text-sm">{plan.ideal}</p>
            </div>

            <div class="text-center mb-6">
              <span class="text-4xl font-bold text-slate-900">{plan.price}</span>
              <span class="text-slate-500">{plan.period}</span>
              <p class="text-xs text-green-600 mt-1 font-semibold">{plan.savings}</p>
            </div>

            <ul class="space-y-3 mb-6">
              {plan.features.map((feature) => (
                <li class="flex items-center gap-3 text-slate-600">
                  <svg class="w-5 h-5 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                  </svg>
                  <span class="text-sm">{feature}</span>
                </li>
              ))}
            </ul>

            <a 
              href="#contacto" 
              class={`block text-center py-3 rounded-xl font-semibold transition-all ${
                plan.popular 
                  ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white hover:from-blue-700 hover:to-blue-800 shadow-lg shadow-blue-500/25' 
                  : 'bg-slate-100 text-slate-700 hover:bg-slate-200'
              }`}
            >
              {plan.cta}
            </a>

            <button 
              data-modal={`modal-anual-${index}`}
              class="w-full mt-3 text-center text-sm text-blue-600 hover:text-blue-700 font-medium"
            >
              Ver detalles completos ->
            </button>
          </div>
        ))}
      </div>
    </div>

    <!-- Tab Content: Pago Unico -->
    <div id="tab-unico" class="tab-content hidden">
      <div class="grid md:grid-cols-2 gap-6 lg:gap-8 max-w-4xl mx-auto">
        {oneTimePlans.map((plan, index) => (
          <div class={`relative bg-white rounded-2xl p-6 lg:p-8 border-2 ${plan.popular ? 'border-blue-500 shadow-2xl shadow-blue-500/10' : 'border-slate-200'} transition-all hover:shadow-xl`}>
            {plan.popular && (
              <div class="absolute -top-4 left-1/2 -translate-x-1/2">
                <span class="px-4 py-1.5 bg-blue-600 text-white text-sm font-semibold rounded-full">
                  Recomendado
                </span>
              </div>
            )}

            <div class="text-center mb-6">
              <h3 class="text-xl font-bold text-slate-900 mb-1">{plan.name}</h3>
              <p class="text-slate-500 text-sm">{plan.ideal}</p>
            </div>

            <div class="text-center mb-6">
              <span class="text-4xl font-bold text-slate-900">{plan.price}</span>
              <span class="text-slate-500 text-sm block">{plan.period}</span>
            </div>

            <ul class="space-y-3 mb-6">
              {plan.features.map((feature) => (
                <li class="flex items-center gap-3 text-slate-600">
                  <svg class="w-5 h-5 text-green-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                  </svg>
                  <span class="text-sm">{feature}</span>
                </li>
              ))}
            </ul>

            <a 
              href="#contacto" 
              class={`block text-center py-3 rounded-xl font-semibold transition-all ${
                plan.popular 
                  ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white hover:from-blue-700 hover:to-blue-800 shadow-lg shadow-blue-500/25' 
                  : 'bg-slate-100 text-slate-700 hover:bg-slate-200'
              }`}
            >
              {plan.cta}
            </a>

            <button 
              data-modal={`modal-unico-${index}`}
              class="w-full mt-3 text-center text-sm text-blue-600 hover:text-blue-700 font-medium"
            >
              Ver detalles completos ->
            </button>
          </div>
        ))}
      </div>
    </div>

    <!-- Tab Content: Personalizado -->
    <div id="tab-personalizado" class="tab-content hidden">
      <div class="max-w-3xl mx-auto">
        <div class="bg-gradient-to-br from-slate-900 to-slate-800 rounded-2xl p-8 lg:p-12 text-center">
          <div class="w-16 h-16 bg-orange-500 rounded-2xl flex items-center justify-center mx-auto mb-6">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
          </div>
          <h3 class="text-2xl lg:text-3xl font-bold text-white mb-4">
            ¿Necesitas Automatización o Software Complejo?
          </h3>
          <p class="text-slate-300 mb-8 max-w-xl mx-auto">
            Para bots de automatización, integraciones de API, sistemas SaaS a medida, o proyectos que requieren ingeniería especializada en Rust y AWS.
          </p>
          
          <div class="grid sm:grid-cols-3 gap-4 mb-8 text-left">
            <div class="bg-white/10 rounded-xl p-4">
              <h4 class="text-white font-semibold mb-2">🤖 RPA & Bots</h4>
              <p class="text-slate-400 text-sm">Automatización de procesos repetitivos con Python</p>
            </div>
            <div class="bg-white/10 rounded-xl p-4">
              <h4 class="text-white font-semibold mb-2">🔗 Integraciones</h4>
              <p class="text-slate-400 text-sm">Conexión con APIs externas y sistemas legados</p>
            </div>
            <div class="bg-white/10 rounded-xl p-4">
              <h4 class="text-white font-semibold mb-2">☁️ SaaS Custom</h4>
              <p class="text-slate-400 text-sm">Desarrollo de plataformas escalables en la nube</p>
            </div>
          </div>

          <a 
            href="#contacto" 
            class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-orange-500 text-white font-semibold rounded-xl hover:bg-orange-600 transition-all shadow-lg shadow-orange-500/25"
          >
            Solicitar Cotización de Ingeniería
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
        </div>
      </div>
    </div>

    <!-- Trust Info -->
    <div class="mt-12 text-center">
      <div class="inline-flex items-center gap-4 flex-wrap justify-center text-sm text-slate-500">
        <span class="flex items-center gap-2">
          <svg class="w-5 h-5 text-green-500" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          Factura electrónica
        </span>
        <span class="flex items-center gap-2">
          <svg class="w-5 h-5 text-green-500" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          Sin contratos largos
        </span>
        <span class="flex items-center gap-2">
          <svg class="w-5 h-5 text-green-500" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
          Tecnología superior a WordPress
        </span>
      </div>
    </div>
  </div>
</section>

<!-- Modal Template -->
<div id="pricing-modal" class="fixed inset-0 z-50 hidden items-center justify-center p-4 bg-black/50 backdrop-blur-sm">
  <div class="bg-white rounded-2xl max-w-lg w-full p-6 lg:p-8 shadow-2xl relative max-h-[90vh] overflow-y-auto">
    <button id="close-modal" class="absolute top-4 right-4 text-slate-400 hover:text-slate-600">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
    
    <h3 id="modal-title" class="text-xl font-bold text-slate-900 mb-4">Detalles del Plan</h3>
    
    <div id="modal-content" class="space-y-4">
      <!-- Content will be injected via JavaScript -->
    </div>

    <div class="mt-6 pt-6 border-t border-slate-100">
      <h4 class="font-semibold text-slate-900 mb-3">¿Por qué somos mejores que WordPress?</h4>
      <ul class="space-y-2 text-sm text-slate-600">
        <li class="flex items-center gap-2">
          <span class="text-green-500">✔</span> Sin plugins vulnerables que actualizar
        </li>
        <li class="flex items-center gap-2">
          <span class="text-green-500">⚡</span> Carga en 0.5 segundos vs 3+ segundos
        </li>
        <li class="flex items-center gap-2">
          <span class="text-green-500">💰</span> Costos de hosting hasta 90% menores
        </li>
        <li class="flex items-center gap-2">
          <span class="text-green-500">🛡️</span> Seguridad de nivel empresarial
        </li>
      </ul>
    </div>

    <a 
      href="#contacto" 
      class="block w-full mt-6 text-center py-3 bg-blue-600 text-white font-semibold rounded-xl hover:bg-blue-700 transition-all"
      onclick="document.getElementById('pricing-modal').classList.add('hidden'); document.getElementById('pricing-modal').classList.remove('flex');"
    >
      Contratar Ahora
    </a>
  </div>
</div>

<style>
  .tab-btn {
    color: #64748b;
    background: transparent;
  }
  .tab-btn:hover {
    color: #1e40af;
    background: #eff6ff;
  }
  .tab-btn.active {
    color: white;
    background: linear-gradient(135deg, #2563eb, #1d4ed8);
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Tab switching
    const tabButtons = document.querySelectorAll('.tab-btn');
    const tabContents = document.querySelectorAll('.tab-content');

    tabButtons.forEach(button => {
      button.addEventListener('click', () => {
        const tabId = button.getAttribute('data-tab');
        
        // Update buttons
        tabButtons.forEach(btn => btn.classList.remove('active'));
        button.classList.add('active');
        
        // Update content
        tabContents.forEach(content => {
          content.classList.add('hidden');
        });
        document.getElementById(`tab-${tabId}`)?.classList.remove('hidden');
      });
    });

    // Modal functionality
    const modal = document.getElementById('pricing-modal');
    const modalTitle = document.getElementById('modal-title');
    const modalContent = document.getElementById('modal-content');
    const closeModal = document.getElementById('close-modal');

    const plansData: Record<string, { name: string; details: string[] }> = {
      'modal-mensual-0': { name: 'Plan Despegue - Mensual', details: ['Sin cuota de alta ni contratos largos', 'Carga en menos de 0.5 segundos', 'Sin plugins vulnerables', 'Soporte por email', 'Cambios menores incluidos'] },
      'modal-mensual-1': { name: 'Plan Crecimiento - Mensual', details: ['Panel de administración intuitivo', 'Optimización para Google', 'Hasta 5 correos @tuempresa.cl', 'Backups automáticos', 'Soporte prioritario'] },
      'modal-mensual-2': { name: 'Catálogo Pro - Mensual', details: ['Gestión de inventario básica', 'Filtros de búsqueda avanzados', 'Galería de imágenes optimizada', 'Notificaciones de pedidos', 'Reportes de visitas'] },
      'modal-anual-0': { name: 'Despegue Anual', details: ['Ahorro de $39.880 vs mensual', 'Carga en menos de 0.5 segundos', 'Sin plugins vulnerables', 'Soporte por email', 'Cambios menores incluidos'] },
      'modal-anual-1': { name: 'Crecimiento Anual', details: ['Ahorro de $69.890 vs mensual', 'Panel de administración intuitivo', 'Optimización para Google', 'Hasta 5 correos @tuempresa.cl', 'Soporte prioritario 24/7'] },
      'modal-anual-2': { name: 'Catálogo Pro Anual', details: ['Ahorro de $99.890 vs mensual', 'Gestión de inventario', 'Filtros avanzados', 'Notificaciones de pedidos', 'Reportes completos'] },
      'modal-unico-0': { name: 'Landing Page Propia', details: ['Propiedad total del código', 'Sin dependencias de terceros', 'Arquitectura escalable', 'Hosting por tu cuenta o gestionado (1 UF/mes)', 'Mantenimiento opcional'] },
      'modal-unico-1': { name: 'Corporativo A Medida', details: ['Desarrollo completo Rust + AWS', 'Base de datos DynamoDB/PostgreSQL', 'API REST documentada', 'Capacitación incluida', 'Soporte post-lanzamiento 30 días'] },
    };

    document.querySelectorAll('[data-modal]').forEach(button => {
      button.addEventListener('click', () => {
        const modalId = button.getAttribute('data-modal');
        if (modalId && plansData[modalId]) {
          const plan = plansData[modalId];
          if (modalTitle) modalTitle.textContent = plan.name;
          if (modalContent) {
            modalContent.innerHTML = `
              <ul class="space-y-3">
                ${plan.details.map(detail => `
                  <li class="flex items-center gap-3 text-slate-600">
                    <svg class="w-5 h-5 text-blue-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                    </svg>
                    <span>${detail}</span>
                  </li>
                `).join('')}
              </ul>
            `;
          }
          modal?.classList.remove('hidden');
          modal?.classList.add('flex');
        }
      });
    });

    closeModal?.addEventListener('click', () => {
      modal?.classList.add('hidden');
      modal?.classList.remove('flex');
    });

    modal?.addEventListener('click', (e) => {
      if (e.target === modal) {
        modal.classList.add('hidden');
        modal.classList.remove('flex');
      }
    });
  });
</script>
